<odoo>
    <!-- Acción para abrir las vistas del modelo school.grado -->
    <record id="action_grados" model="ir.actions.act_window">
        <field name="name">Grados</field>
        <field name="res_model">school.grado</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Vista de lista (tree) para el modelo school.grado -->
    <record id="view_tree_grados" model="ir.ui.view">
        <field name="name">Grados Tree</field>
        <field name="model">school.grado</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name" />
            </tree>
        </field>
    </record>

    <!-- Vista de formulario (form) para el modelo school.grado -->
    <record id="view_form_grados" model="ir.ui.view">
        <field name="name">Grados Form</field>
        <field name="model">school.grado</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>
</odoo>